## with GPT

## RSA diagram

(키 생성 → 암호화 → 복호화/서명)

1) RSA 키 생성 도식
[랜덤 소수 선택]
   p           q
   |           |
   +----곱-----+
        n = p*q  ---------------------> (공개키/개인키에 같이 들어감)
        |
        +--> φ(n) = (p-1)(q-1)
                   |
                   +--> e 선택 (1 < e < φ(n), gcd(e, φ(n))=1)
                   |
                   +--> d 계산 (d ≡ e^{-1} mod φ(n))
                        (즉, e*d ≡ 1 (mod φ(n)))

[공개키]  (n, e)
[개인키]  (n, d)   + (p, q)는 보통 내부적으로도 보관/활용


2) RSA 암호화/복호화 도식 (기밀성)
암호화(보내는 사람 → 받는 사람)
평문 M
  |
  |  받는 사람 공개키 (n, e)
  v
C = M^e mod n
  |
  v
암호문 C 전송
복호화(받는 사람)
암호문 C
  |
  |  받는 사람 개인키 (n, d)
  v
M = C^d mod n
  |
  v
원문 M 복원

* 핵심: 공개키로 암호화, 개인키로 복호화


3) RSA 서명/검증 도식 (무결성 + 인증)
서명(보내는 사람)
메시지 M
  |
  +--> 해시: h = H(M)
           |
           |  보내는 사람 개인키 (n, d)
           v
서명 S = h^d mod n
  |
  +--> (M, S) 전송
검증(받는 사람)
수신한 (M, S)
  |
  +--> h1 = H(M)
  |
  |  보내는 사람 공개키 (n, e)
  v
h2 = S^e mod n
  |
  +--> h1 == h2 이면 "서명 유효"

* 핵심: 개인키로 서명, 공개키로 검증


# Summary
기밀성(암호화):
  Sender:   M --(Receiver 공개키)--> C
  Receiver: C --(Receiver 개인키)--> M

무결성/인증(서명):
  Signer:   M -> H(M)=h --(Signer 개인키)--> S
  Verifier: M -> H(M)=h1,  S --(Signer 공개키)--> h2,  h1==h2 ?
